Convert the following HTML into a production-ready **React + TypeScript** component.

### Tech Stack

* React + TypeScript
* Tailwind CSS
* ShadCN UI components where appropriate
* Lucide React icons (replace Material Symbols)
* React Router (`Link`) if navigation is needed.

### UI Requirements

* Preserve the design **exactly** as provided.
* Keep the same layout, width, height, spacing, padding, margins, borders, border radius, colors, shadows, and responsiveness.
* Do not redesign or simplify the UI.

### Styling

* Use the CSS variables already defined in `index.css`.
* Do not hardcode colors.

Available theme variables:

```css
primary
primary-foreground
primary-container
secondary
secondary-foreground
accent
accent-foreground
surface
surface-container-lowest
on-surface-variant
on-background
border
input
outline
outline-variant

and use color and vaible like this eg=> text-primary, bg-secondary not like text-[hsl(var(--primary-foreground))]

### Components

* Use ShadCN components (`Card`, `Button`, `Input`, `Label`, etc.) whenever suitable.
* Replace Material icons with equivalent Lucide React icons.
* Add `cursor-pointer` to every clickable element.

### Code Quality

* Generate complete, production-ready code.
* Include all imports and state management.
* Keep the code clean, reusable, and fully typed.
* Do not leave placeholder code.

Return a single complete `.tsx` component that is ready to use.

Now convert the following HTML:


